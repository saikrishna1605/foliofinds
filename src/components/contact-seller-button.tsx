"use client";

// This component is not currently in use.
// Direct messaging has been replaced by the tawk.to widget for live support
// and the AI chatbot for general questions.
export function ContactSellerButton({ sellerId }: { sellerId: string }) {
    return null;
}
